[build]
  base    = "delta-rio"
  command = "npm run build"
  publish = "delta-rio/.next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS    = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"
